//This file contains preloaded folder (and possibly other) data to improve initial user experience
localCollection = new Meteor.Collection;

//format of data is { originalCollection: "[original collection name]", data: [original document] }
//INSERTDATABELOW
